#include "pch.h"
#include "Fields.h"

namespace Alalba
{
	
	//const float ScalarField::eval(const lux::Vector& P) const
	//{
	//	// mask
	//	return m_sdf->eval(P) <= 0.0? 0.0 : 1.0;
	//}
	//const lux::Color ColorField::eval(const lux::Vector& P) const
	//{
	//	// return m_sdf->eval(P) <= 0.0 ? lux::Color() : m_baseColor* m_sdf->eval(P);
	//	return m_sdf->eval(P) <= 0.0 ? lux::Color() : m_baseColor * 1.0;
	//}
	
}